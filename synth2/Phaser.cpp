#include "Phaser.hpp"

void set(uint8_t min_delay, uint8_t max_delay, uint8_t speed)
{
    this->min_delay = min_delay;
    this->max_delay = max_delay;
    this->cur_pos = 

}

void readIn(buffers::BufferID sourceBuffer)
{

}

void render(buffers::BufferID destBuffer)
{
    for (int i=0; i<buffers::SAMPLES_PER_BUFFER; i++)
    {


        this->cur_pos +=         
    }
}
